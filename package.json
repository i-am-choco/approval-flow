{
  "name": "root-approval-flow",
  "version": "0.0.1",
  "description": "Just a component for approval flow",
  "files": [
    "dist"
  ],
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:approval-flow": "lerna exec --scope=@choco_leong/approval-flow -- npm run build",
    "dev:example": "lerna exec --scope=example -- npm run start",
    "lint": "lerna exec -- eslint ."
  },
  "author": "Choco Leong",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^7.4.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}

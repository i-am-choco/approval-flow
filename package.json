{
  "name": "root-approval-flow",
  "version": "0.0.1",
  "description": "Just a component for approval flow",
  "files": [
    "dist"
  ],
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:approval-flow": "lerna exec --scope=approval-flow -- npm run dev",
    "dev:custom-form": "lerna exec --scope=custom-form -- npm run dev",
    "dev:example": "lerna exec --scope=example -- npm run start",
    "build:approval-flow": "lerna exec --scope=approval-flow -- npm run build",
    "build:custom-form": "lerna exec --scope=custom-form -- npm run build",
    "lint": "lerna exec -- eslint ."
  },
  "author": "Choco Leong",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^7.4.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
